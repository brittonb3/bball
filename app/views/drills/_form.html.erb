<%= form_for @drill, html: { multipart: true } do |f| %>
   <%= f.label :file %>
   <%= f.file_field :file %>

   <%= f.label :title %>
   <%= f.text_field :title %>

   <%= f.label :description %>
   <%= f.text_area :description %>

   <%= f.submit "Save" %>
 <% end %>

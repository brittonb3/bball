<h1>Drills#show</h1>
<%= link_to "Edit", edit_drill_path(@drill), class: 'btn btn-success' %>
<%= link_to "Delete Drill", @drill, method: :delete, class: 'btn btn-danger', data: {confirm: 'Are you sure you want to delete this drill?'} %>
<h1><%= @drill.title %></h1>
 <p><%= @drill.description %></p>
